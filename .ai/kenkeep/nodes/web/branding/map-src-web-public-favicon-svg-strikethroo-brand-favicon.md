---
schema_version: 2
id: map-src-web-public-favicon-svg-strikethroo-brand-favicon
title: src/web/public/favicon.svg — Strikethroo brand favicon
kind: map
tags:
  - web
  - assets
  - favicon
  - brand
derived_from: []
relates_to: []
confidence: high
summary: >-
  SVG favicon: ink rounded-square, cream lowercase `s` from Outfit weight-400
  glyph outline, with thin strike-through.
---
The favicon at `src/web/public/favicon.svg` is the Strikethroo compact mark: a cream (`#f7f3ec`) lowercase `s` on a black (`#19110a`) rounded-square container with a thin strike-through bar. The `s` glyph is the actual Outfit Regular (weight 400) outline extracted via `fontkit`. Vite copies `src/web/public/` to `dist-web/` at build time.
