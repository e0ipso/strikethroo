Run `scripts/check-for-updates.cjs` as a separate command from this skill's
`scripts/` directory — not through `find-strikethroo-root.cjs`. Read the one
JSON line on stdout. When `notice` is present, retain its exact text for the
final user-facing response. Never pause for permission and never run an update.
