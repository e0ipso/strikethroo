/**
 * Smoke-app root. Task 004 fills this with a Dalia Button + Fraunces heading
 * to verify the toolchain and vendored design system render correctly. This is
 * throwaway verification scaffolding, superseded by the real app shell later.
 */
export function App() {
  return <div id="app" />;
}
